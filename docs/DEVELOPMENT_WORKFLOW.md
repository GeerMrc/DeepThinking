# DeepThinking-MCP 开发流程规范

> **版本**: 1.0
> **创建日期**: 2026-01-01
> **维护者**: Maric
> **适用范围**: DeepThinking-MCP 项目开发

---

## 📋 文档说明

### 文档目的

建立标准的开发流程规范，确保代码质量、测试覆盖率和文档同步更新。

### 核心原则 ⭐

1. **单一任务追踪文档原则**
   - **唯一任务追踪源**: `TASKS.md` 是整个项目开发的唯一任务追踪文档
   - **迭代式更新**: 所有新阶段、新任务都在 `TASKS.md` 中迭代式添加
   - **禁止创建冗余计划文档**: 不允许创建额外的 `PHASE_XX_PLAN.md` 等计划文档

2. **文档管理优先级原则**
   - **优先更新现有文档**: 创建新文档前必须先检查是否存在相关文档
   - **避免文档混乱**: 禁止创建大量功能重复的文档
   - **文档整合**: 相关内容应整合到现有文档中

3. **安全开发原则**
   - **禁止高危命令**: 严格禁止使用 `rm -rf` 等高危命令
   - **安全删除操作**: 删除文件/目录时必须使用安全的替代方式

### 适用人员

- 主要开发者：GLM-4.7
- 项目维护者：Maric
- 代码审查者：Maric

---

## 🔄 标准化开发流程

### 整体流程图

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  需求分析   │───▶│  方案设计   │───▶│  任务拆解   │
└─────────────┘    └─────────────┘    └─────────────┘
                                             │
                                             ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  Git 提交   │◀───│  代码审查   │◀───│  测试验证   │
└─────────────┘    └─────────────┘    └─────────────┘
                                             ▲
                                             │
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  文档更新   │◀───│  部署发布   │◀───│  代码实现   │
└─────────────┘    └─────────────┘    └─────────────┘
```

### 阶段详细说明

#### 1. 需求分析阶段

**输入**: 用户需求、Bug 报告、功能请求

**活动**:
- 理解需求背景和目标
- 技术调研和可行性分析
- 确定需求优先级（P0/P1/P2）

**输出**:
- 需求分析文档
- 技术调研报告

**无需 Git 操作**

---

#### 2. 方案设计阶段

**活动**:
- 设计技术方案
- 架构设计
- API 设计
- 数据模型设计

**输出**:
- 技术方案文档
- 架构设计文档

**无需 Git 操作**

---

#### 3. 任务拆解阶段

**活动**:
- 将功能拆解为具体任务
- 估算任务工作量
- 创建 TODO 清单
- 确定任务依赖关系

**输出**:
- TODO 清单
- 任务排期

**无需 Git 操作**

---

#### 4. 代码实现阶段

**活动**:
- 按照 TODO 清单逐项实现
- 使用 TodoWrite 工具实时跟踪任务状态
- 每完成一个子任务更新状态

**开发规范**:
- 遵循代码风格规范（ruff）
- 添加类型注解（mypy）
- 编写中文 JSDoc 注释
- 错误处理完整

**任务状态管理**:
```python
# 任务状态流转
pending → in_progress → completed
                          ↓
                       failed → pending
```

**输出**:
- 完成的代码
- 更新的 TODO 清单

**无需 Git 操作**

---

#### 5. 测试验证阶段

**活动**:
- 编写单元测试
- 编写集成测试
- 执行完整测试套件
- 检查测试覆盖率

**质量标准**:
- 单元测试覆盖率 > 80%
- 所有公共方法必须有测试
- 边界条件和异常情况必须覆盖

**测试命令**:
```bash
# 运行所有测试
pytest

# 运行测试并生成覆盖率报告
pytest --cov=deep_thinking --cov-report=html

# 运行特定测试
pytest tests/test_tools/test_sequential_thinking.py
```

**输出**:
- 测试报告
- 覆盖率报告

**无需 Git 操作**

---

#### 6. 代码审查阶段

**活动**:
- 自我代码审查
- 静态代码分析
- 类型检查
- 安全审查

**检查命令**:
```bash
# Ruff 代码检查
ruff check src/ tests/

# Ruff 自动修复
ruff check --fix src/ tests/

# Mypy 类型检查
mypy src/deep_thinking/

# 安全检查（如有）
bandit -r src/deep_thinking/
```

**质量标准**:
- 无 ruff 错误
- 无 mypy 类型错误
- 无已知安全漏洞

**输出**:
- 修复后的代码

**无需 Git 操作**

---

#### 7. 文档更新阶段

**活动**:
- 更新 API 文档
- 更新用户指南
- 更新架构文档
- 更新 CHANGELOG

**文档管理原则**:
1. **单一任务追踪源**: `TASKS.md` 是整个项目开发的唯一任务追踪文档
   - 所有新阶段、新任务都在 `TASKS.md` 中迭代式添加
   - 禁止创建额外的 `PHASE_XX_PLAN.md`、`ROADMAP.md` 等冗余计划文档
   - 阶段计划直接在 `TASKS.md` 中描述

2. **优先更新现有文档**，避免创建重复文档
3. **文档创建决策流程**:
   ```
   是否存在相关文档？
   ├── 是 → 直接更新现有文档
   ├── 部分相关 → 在现有文档中添加新章节
   └── 无相关文档 → 创建新文档，但必须：
       ├── 检查是否可以归类到现有目录结构
       ├── 使用标准命名规范
       ├── 添加到文档索引中
       └── 在相关文档中建立交叉引用
   ```
4. 检查清单：
   - [ ] 搜索现有文档（Glob 工具）
   - [ ] 确定文档类型
   - [ ] 选择合适目录
   - [ ] 使用标准命名
   - [ ] 添加到文档索引
   - [ ] 更新 TASKS.md（如涉及任务变更）

**文档更新检查**:
```bash
# 检查文档链接
markdown-link-check docs/**/*.md

# 检查文档格式
markdownlint docs/**/*.md
```

**输出**:
- 更新的文档

**可选 Git 操作**

---

#### 8. Git 提交阶段 ⚠️ **关键**

**提交前检查清单**:
- [ ] 代码质量检查通过（ruff + mypy）
- [ ] 单元测试全部通过
- [ ] 测试覆盖率 > 80%
- [ ] 文档已同步更新
- [ ] 无调试代码残留
- [ ] 无敏感信息泄露

**提交规范**:
```bash
# 1. 查看修改状态
git status

# 2. 添加修改文件
git add <files>

# 3. 提交（使用 Conventional Commits 规范）
git commit -m "type(scope): description

详细说明（可选）

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

**提交类型**:
- `feat`: 新功能
- `fix`: 问题修复
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建工具或辅助工具的变动

**示例**:
```bash
# 功能开发提交
git commit -m "feat(config): 添加思考步骤限制配置支持

- 添加环境变量 DEEP_THINKING_MAX_THOUGHTS
- 添加 CLI 参数 --max-thoughts
- 添加边界检查逻辑

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"

# 文档更新提交
git commit -m "docs(sse): 添加SSE配置详细指南

- 创建 docs/sse-guide.md
- 添加认证机制说明
- 添加故障排除章节

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

**输出**:
- Git 提交记录
- 更新的 TASKS.md

**必须 Git 操作**

---

#### 9. 部署发布阶段

**活动**:
- 版本号更新
- 构建发布包
- 测试发布包
- 发布到 PyPI（手动）

**发布检查清单**:
```bash
# 1. 版本号检查
grep version pyproject.toml

# 2. 构建发布包
python -m build

# 3. 检查发布包
twine check dist/*

# 4. 测试本地安装
pip install dist/deep_thinking_mcp-*.whl

# 5. 发布到 PyPI（手动）
twine upload dist/*
```

**输出**:
- 发布包
- 发布说明

**必须 Git 操作**（打标签）

---

## 📊 阶段完成标准

### 每个阶段完成后的检查点

| 阶段 | 检查项 | 验证方式 | 通过标准 |
|------|--------|----------|----------|
| 需求分析 | 需求明确 | 需求文档 | 需求清晰可执行 |
| 方案设计 | 设计完整 | 设计文档 | 方案可行无漏洞 |
| 任务拆解 | 任务详细 | TODO 清单 | 任务可追踪 |
| 代码实现 | 代码完成 | 代码审查 | 符合编码规范 |
| 测试验证 | 测试通过 | pytest | 覆盖率 >80% |
| 代码审查 | 质量合格 | ruff + mypy | 无错误无警告 |
| 文档更新 | 文档完整 | 文档检查 | 文档与代码一致 |
| Git 提交 | 提交完成 | git status | 已提交到本地仓库 |

### 最终验收标准

| 检查项 | 验证方式 | 通过标准 |
|--------|----------|----------|
| 功能完整 | 功能测试 | 所有功能正常 |
| 代码质量 | ruff + mypy | 无错误无警告 |
| 测试覆盖 | pytest --cov | 覆盖率 >80% |
| 文档完整 | 文档审查 | 所有文档已更新 |
| Git 提交 | git log | 提交记录清晰 |

---

## 🚫 开发红线

### 禁止行为

1. **禁止高危命令** ⚠️ **严重**
   - **严格禁止 `rm -rf`**：这是极其危险的命令，可能导致不可逆的数据丢失
   - **禁止 `rm -rf /`、`rm -rf *`、`rm -rf ~`**：任何形式的强制递归删除根目录或当前目录
   - **禁止 `dd` 命令直接写盘**：可能导致磁盘数据彻底丢失
   - **禁止 `:(){ :|:& };:`**：fork 炸弹，可能导致系统崩溃

2. **禁止跳过测试**
   - 代码修改后必须运行测试
   - 新功能必须包含测试

3. **禁止虚假进度**
   - 禁止标记任务完成但实际未完成
   - 禁止跳过开发流程阶段

4. **禁止提交未审查代码**
   - 必须通过 ruff 和 mypy 检查
   - 必须通过所有测试

5. **禁止文档滞后**
   - 代码变更必须同步更新文档
   - API 变更必须更新 API 文档

6. **禁止创建冗余文档**
   - 禁止创建 `PHASE_XX_PLAN.md` 等额外计划文档
   - 所有任务追踪必须在 `TASKS.md` 中迭代式更新

---

### 安全删除操作规范 ✅

**文件删除安全操作**：

```bash
# ❌ 禁止：高危命令
rm -rf filename.md
rm -rf directory/
rm -rf *

# ✅ 推荐：安全删除文件
cd /path/to/directory
rm filename.md

# ✅ 推荐：安全删除目录（先确认内容）
cd /path/to/parent
ls -la directory/  # 先查看目录内容
rm -r directory/

# ✅ 推荐：使用 trash 命令（更安全，可恢复）
trash filename.md
trash directory/
```

**删除前确认清单**：
- [ ] 确认文件/目录路径正确
- [ ] 确认不再需要该文件/目录
- [ ] 确认已备份重要数据
- [ ] 使用 `ls` 或 `find` 先查看内容
- [ ] 使用 `rm` 而非 `rm -rf`（单个文件）
- [ ] 使用 `rm -r` 而非 `rm -rf`（目录）

**推荐使用 Python 的 os/pathlib 进行文件操作**：
```python
from pathlib import Path

# 安全删除文件（Python 方式）
file_path = Path("docs/PHASE11_PLAN.md")
if file_path.exists():
    file_path.unlink()
    print(f"已删除: {file_path}")
```

---

## 📝 TODO 清单管理

### TodoWrite 工具使用

**创建任务清单**:
```python
TodoWrite(
    todos=[
        {
            "content": "实现功能A",
            "status": "pending",
            "activeForm": "实现功能A中"
        },
        {
            "content": "编写测试A",
            "status": "pending",
            "activeForm": "编写测试A中"
        }
    ]
)
```

**更新任务状态**:
```python
# 开始任务
TodoWrite(todos=[...], status="in_progress")

# 完成任务
TodoWrite(todos=[...], status="completed")
```

### 任务状态流转

```
pending → in_progress → completed
           ↓               ↓
        failed ───────→ pending
```

---

## 📋 Git 工作流

### 分支管理

| 分支 | 用途 | 生命周期 |
|------|------|----------|
| `master` | 主分支（生产代码） | 长期 |
| `develop` | 开发分支 | 长期 |
| `feature/xxx` | 功能分支 | 临时 |
| `fix/xxx` | 修复分支 | 临时 |

### 提交流程

```
开发 → 提交到本地 master → 推送到远程 → 合并到 master
```

### 阶段提交流程

每个开发阶段完成后：
1. 运行完整测试套件
2. 运行代码质量检查
3. 更新相关文档
4. 更新 TASKS.md
5. 提交到 Git
6. 推送到远程（可选）

---

## 🔄 持续改进

### 流程回顾

每个阶段结束后进行回顾：
- 哪些做得好？
- 哪些需要改进？
- 下次如何做得更好？

### 流程优化

根据实际情况优化流程：
- 调整任务粒度
- 优化检查点
- 改进工具使用

---

## 📚 参考资料

- [全局配置文件](../../CLAUDE.md)
- [项目任务追踪](../TASKS.md)
- [架构设计文档](../ARCHITECTURE.md)
- [API 文档](./api.md)
- [安装配置指南](./installation.md)

---

## 📞 联系方式

- **维护者**: Maric
- **主要开发者**: GLM-4.7
- **问题反馈**: GitHub Issues

---

> **重要提醒**: 本规范是确保代码质量和开发效率的重要文档，请严格按照规范执行，并根据实际需要进行适当的调整和优化。
